package com.logbookmanager.service;

import com.logbookmanager.domain.model.logbook.Logbook;

public interface LogbookService extends LBMService<Logbook, Long> {
	// Placeholder just in case PersonService needs more
}